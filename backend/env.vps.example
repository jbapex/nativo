# Configuração para conectar ao PostgreSQL da VPS via túnel SSH
# 
# IMPORTANTE: 
# 1. Crie um túnel SSH antes de usar:
#    ssh -L 5433:localhost:5432 root@nativo.contaae.online
# 
# 2. Copie este arquivo para .env.vps:
#    cp env.vps.example .env.vps
#
# 3. Use quando quiser desenvolver conectado à VPS:
#    cp .env.vps .env

# Banco de Dados
DB_TYPE=postgres

# PostgreSQL (via túnel SSH)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=nativo_db
DB_USER=nativo_user
DB_PASSWORD=Nativo2025SecureDB

# Outras configurações (manter do .env original)
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3006

# JWT
JWT_SECRET=seu-secret-super-seguro-aqui-ALTERE-EM-PRODUCAO
JWT_REFRESH_SECRET=

# Upload
MAX_FILE_SIZE=5242880
UPLOAD_DIR=./uploads

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_WINDOW_MS=300000
AUTH_RATE_LIMIT_MAX=20

# Logs
LOG_LEVEL=info
LOG_FILE=./logs/app.log

